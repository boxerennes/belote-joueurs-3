// Service Worker minimal pour permettre l'installation PWA
self.addEventListener('fetch', function(event) {
    // Ce code permet à l'application de fonctionner même avec une connexion instable
});